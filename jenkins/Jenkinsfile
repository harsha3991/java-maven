pipeline {
    agent any
    stages {            
        stage('javadoc') {
            steps {                
                sh '''
                mkdir -p src/javadoc/
                cd src/javadoc/
                javadoc ../* -subpackages *.java
                javadoc App.java'''
            }
        }        
    }
}
