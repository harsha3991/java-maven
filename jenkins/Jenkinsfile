pipeline {
    agent any
    stages {
        stage('javadoc') {
            steps {
                sh 'javadoc -sourcepath src/main/java/com/mycompany/app/ '
            }
        }        
    }
}
