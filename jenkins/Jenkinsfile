pipeline {
    agent any
    stages {            
        stage('javadoc') {
            steps {                
                sh '''
                mkdir -p javadoc/
                cd javadoc/
                javadoc ../src/* -subpackages *.java
                javadoc App.java'''
            }
        }        
    }
}
